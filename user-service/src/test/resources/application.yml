spring:
  datasource:
    url=jdbc:h2:mem:user-test-db;DB_CLOSE_DELAY=-1:
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect